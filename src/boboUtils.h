#ifndef BOBO_UTILS_H
#define BOBO_UTILS_H

#include <vector>
#include <string>

std::vector<std::string> readFile(const std::string &fileName);

long parseLong(const std::string &s);

#endif // BOBO_UTILS_H
